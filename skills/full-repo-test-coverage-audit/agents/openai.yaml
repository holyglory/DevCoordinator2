interface:
  display_name: "Full Repo Test Coverage Audit"
  short_description: "Audit complete coverage and efficient test execution"
  default_prompt: "Use $full-repo-test-coverage-audit to assess complete code and UI coverage and efficient focused testing, inherit parent settings for all workers without overrides, and return independent evidence-backed verdicts with artifact links."
policy:
  allow_implicit_invocation: false
