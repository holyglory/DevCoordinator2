# User Issue Ledger: UI / navigation

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-UI-NAVIGATION-001 | DevCoordinator2 Console global header, destination headings, and repository-scoped Plan, Decisions, and Codex Usage pages | The agent shipped a wrapping multi-row global header, rendered detail destination names as inert headings, and exposed the current repository without a direct same-destination project switcher | Keep the global shell on one row at every supported width, replacing the inline navigation with an accessible hamburger menu before it would wrap; make every destination title a real link back to its collection; on repository-scoped destinations, show the current project beside a small button that opens a custom DOM menu of every project visible on that destination | Inventory every route and state; assert headings contain the correct anchor, repository menus are non-native DOM controls with real project links, mouse and keyboard open/navigate/escape behavior works, focus returns after closing, the selected project changes route, the header never wraps or overflows at breakpoint-minus-one/at/plus-one and narrow/wide widths, and all existing page journeys still pass |
