# User Issue Ledger: UI / health

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-UI-HEALTH-001 | Health overview summary, alerts, and unhealthy deployments at wide and narrow viewports | The page gave host capacity, routine counts, and urgent failures equal visual weight in one auto-flowing tile grid. Tall tiles set uneven row heights, leaving large empty areas and making the operational story difficult to scan. | Lead with a compact, aligned host-capacity group, then show operational counts with urgent states visually distinct. Keep unhealthy deployments and their real lifecycle actions immediately recognizable without stretching shorter cards to match long incident details. | Exercise populated, empty, loading, error, and permission-limited Health states at representative wide and narrow widths. Verify capacity values remain aligned and readable, urgent status precedes lower-priority history and repository detail, cards do not create misleading blank space, and every existing action and range control still works. |
| UIL-UI-HEALTH-002 | Health repository attribution, especially shared and unattributed storage | Flattening every storage category into one non-wrapping table cell widened the repository table until the final categories were cropped; at medium widths, repository paths also wrapped into tall narrow columns that made the collection hard to compare. | Keep each storage category and value together, allow the set to wrap or stack within the available width, and switch the repository comparison to a labelled compact layout before columns become unreadable. Do not require document-level horizontal scrolling to discover attribution details. | Use long repository names, paths, deployment names, and every shared storage category. At 1440 px, the user-marked 856 px viewport, 390 px, and breakpoint-minus-one/at/plus-one, verify every category is visible, labels remain associated with values, paths wrap intentionally, and there is no text clipping or document overflow. |
