# User Issue Ledger: UI / usage analytics

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-UI-USAGE-ANALYTICS-001 | Codex Usage completeness status, repository collection, charts, and measurement details | The agent first exposed implementation terms such as `collector` and `partial coverage`, then replaced them with `usage history` without explaining that the count represents separately configured Codex environments | Lead with the consequence in plain language: say when some usage may be missing, state how many configured Codex environments supplied data, and explain that each environment keeps a separate local usage history while unavailable environments are excluded rather than counted as zero; keep environment identities and per-environment values private | Exercise complete, some-missing, no-measurement, and unavailable fixtures at wide and narrow widths; assert no user-facing `collector`, raw `partial coverage`, or unexplained counted `history` wording remains, the status and explanation precede the charts, exact bucket status is understandable, privacy scans pass, and no text clips, overlaps, or causes document overflow |
