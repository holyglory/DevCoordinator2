# User Issue Ledger: UI / usage analytics

| ID | Applies to | Mistake pattern | Required behavior | Prevention and verification |
| --- | --- | --- | --- | --- |
| UIL-UI-USAGE-ANALYTICS-001 | Codex Usage completeness status, repository collection, charts, and measurement details | The agent first exposed implementation terms such as `collector` and `partial coverage`, then replaced them with `usage history` without explaining that the count represents separately configured Codex environments, and finally left the full explanation permanently inline where it displaced the dashboard | Lead with the consequence in one concise visible status: say when some usage may be missing and state how many configured Codex environments supplied data. Put the definition of an environment and the excluded-not-zero explanation in a compact adjacent pop-up hint instead of a permanent paragraph; keep environment identities and per-environment values private | Exercise complete, some-missing, no-measurement, and unavailable fixtures at wide and narrow widths; assert no user-facing `collector`, raw `partial coverage`, or unexplained counted `history` wording remains; the status precedes the charts while the full explanation is absent until its labelled hint is opened; verify click and keyboard opening, Escape and outside-click dismissal, focus restoration, accessible association, exact bucket status, privacy scans, and no clipping, overlap, or document overflow |
| UIL-UI-USAGE-ANALYTICS-002 | Codex Usage repository collection status | The Console painted repository setup gaps with the same red failure treatment as an actual source read failure, producing a wall of alarming statuses that did not match what happened | Reserve red for a real source/read failure. Show repositories that are not connected in every Codex environment as a compact neutral setup state; keep partial measured data amber, complete data green, and no measurements neutral | Render mixed complete, partial, unobserved, not-connected, and source-failure rows together at wide and marked 858×915 widths; assert distinct wording, dot/color classes, accessible text, no clipped status column, and no implementation reason names |
